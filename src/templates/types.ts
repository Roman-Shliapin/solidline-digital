/** Shared props for demo marketing templates */
export type TemplateProps = {
  businessName?: string;
  tagline?: string;
  /** Hex акценту з поля Brand colors (після resolveBrandColor) */
  accentColor?: string;
};
